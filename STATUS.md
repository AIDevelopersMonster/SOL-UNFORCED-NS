# STATUS

**Project:** SOL-UNFORCED-NS  
**Working version:** v0.8 forward relay architecture with coupled mean reduction  
**Date:** 2026-09-11  
**Branch:** `research/local-beta-relay-v0.2`

## Corrected carrier law

The earlier apparent dyadic-chart frequency jump was retracted. The corrected intrinsic leading carrier scale is

\[
\boxed{\Omega_{\rm phys}(q)\asymp q^{-(1+h)/2}.}
\]

See `proofs/chart_invariant_carrier_scale.md`.

## v0.8 architecture

Freeze one sufficiently large integer relay-design parameter `M` and set

\[
\beta_1=2,\qquad \beta_2=1,\qquad u_*=M^2,
\]

with the half-step relay family from `proofs/integral_beta_v08_large_u_halfstep_family.md`. Exact finite-`u_*` resonances exist along `u_*=M^2->infinity`; the desired difference harmonic is unit-beta; every non-designated principal-growing nonzero lattice mode is action-subcritical; and `T=0` nonzero modes are purely viscous.

The principal action deficit is

\[
\boxed{
S_M(a,b)-\mathcal E_{|T|,M^2}(\xi_M(a,b))\le-\delta_*
}
\]

for some fixed `delta_*>0` after `M` is frozen.

## Source-localized action preservation — CLOSED at finite stage for nonzero harmonics

`proofs/source_localized_action_preservation.md` proves that the v0.8 action gap survives every fixed finite composition of the localized source operations audited from the OpenAI construction. No audited nonzero-harmonic operation creates `exp(+cS_*)`; action-subcritical descendants retain

\[
\boxed{e^{-c_MS_*}.}
\]

This does not by itself control the angular mean generated by conjugate wave pairs.

## Stage-uniform infinite nonzero lattice — stable complement CLOSED

`proofs/finite_critical_block_uniform_stable_inverse.md` proves that for one frozen design the infinite stable complement of the nonzero lattice has a uniform negative gap and quadratic high-mode smoothing:

\[
\boxed{
\|G_\nu f_\nu\|_{\rm pkt}
\le
\frac{C_M}{1+|\nu|^2}
\|f_\nu\|_{\rm pkt}.
}
\]

`proofs/analytic_quadratic_symbol_bound.md` proves the corresponding analytic lattice convolution bound for the nonzero harmonic system.

## Critical block count

`proofs/v08_critical_block_cardinality.md` proves that the nonzero principal critical set is finite for every frozen `M` but grows as

\[
\boxed{|\mathcal C_M|=\Theta(M^5).}
\]

Therefore imposing a separate two-sided compact-support compatibility condition on every critical nonzero mode is mathematically finite but architecturally poor.

## Preferred local formulation — exact forward nonzero-harmonic evolution

`proofs/nonzero_harmonic_forward_reduction.md` absorbs the mixed designated terms into the full linearized evolution

\[
\partial_v z=(\mathcal D+\mathcal K)z+f+\mathcal B(z,z).
\]

Parabolic high-mode smoothing plus a Volterra argument on the bounded relay interval gives a truncation-independent propagator on the nonzero harmonic analytic lattice.

The conservative supported defect size is

\[
\boxed{
\rho_\ell
\lesssim
S_*^C\varepsilon^{1/5}
+S_*^Ce^{-c_MS_*}.
}
\]

For every sufficiently small angular mean input `m`, there is a unique exact nonzero-harmonic forward solution

\[
\boxed{z=z(m)}
\]

with norm `O(rho_ell)`.

The old file `proofs/forward_relay_input_output_closure.md` is now explicitly marked **SUPERSEDED** wherever it claimed full local zero residual. Its surviving content is only the architectural input-output idea.

## Angular mean — current decomposition

`notes/mean_block_zero_force_audit.md` identified the angular Fourier mode `0` as the remaining local zero-force problem. The source mean machinery contains temporal/transport inversion, radial pressure/stress inversion, wave covariance and signed-stress feedback, and an explicit five-dimensional moment correction.

The new blockwise audit is `proofs/mean_nonlinear_block_small_factor_audit.md`.

### 1. Fast-time torus divisor — REMOVED in the forward formulation

`proofs/characteristic_fast_time_mean_inverse.md` proves the exact causal inverse of

\[
\boxed{t_*=-\varepsilon\partial_T+c_iN_i}
\]

on a bounded normalized physical-time strip. In translation-invariant torus norms,

\[
\boxed{
\|\mathcal J_iF\|\le L\|F\|
}
\]

with no Diophantine denominator and no torus derivative loss.

Hence Nash--Moser is not needed merely for the fast-time mean equation.

### 2. Five-dimensional moment block — POSITIVE SMALL GAIN

After the source Vandermonde correction removes the linear moment defects exactly, the nonlinear recomputation gains

\[
\boxed{\varepsilon^{0.9-2\kappa_s}.}
\]

Thus the moment block contributes

\[
\eta_{\rm mom,\ell}
\lesssim C_MS_*^C\varepsilon^{0.9-2\kappa_s}=o(1).
\]

### 3. Mean -> wave -> mean feedback — POSITIVE SMALL GAIN, QUANTITATIVE LEMMA STILL TO WRITE

The source exponent rules give

\[
W_{1/2}\times M_\mu\to W_\mu,
\qquad
W_{1/2}\times W_\mu\to M_{\mu+1/2-\kappa_s}.
\]

Therefore the principal differentiated round trip

\[
\delta m\to\delta z\to\delta W\to\delta m'
\]

has the target small factor

\[
\boxed{\varepsilon^{1/2-\kappa_s}.}
\]

The qualitative local Lipschitz dependence `z=z(m)` is already proved; the exact exponent-preserving difference estimate in the chosen packet/Gevrey norm remains to be written.

### 4. Signed-stress covariance branch — POSITIVE SMALL GAIN

The source stress correction removes the prescribed principal covariance and leaves only higher-order interaction/reconstruction/self-covariance terms. A conservative source-supported gain is

\[
\boxed{\varepsilon^{0.17}.}
\]

This exponent is used only as a safe positive placeholder until the exact source rational/exponent combination is pinned in publication notation.

### 5. Supported residual block — SMALL

The remaining supported algebraic and action-flat pieces contribute at worst

\[
\boxed{C_M\rho_\ell=o(1).}
\]

## Radial compactification — corrected result and new obstruction

The previous file `proofs/gevrey_radial_compactification_remainder.md` overclaimed what follows from the schematic fixed-loss `C^m` estimate. The corrected analysis is in

`proofs/radial_gevrey_radius_loss_and_same_space_obstruction.md`.

Let `M_r` denote the source **radial phase parameter**

\[
M_r\asymp\varepsilon^{-\kappa_s}S_*^{-\rho_g}.
\]

For compactly supported Gevrey-`s>1` data, the exact radial phase-following operator admits the Fourier sampling formula

\[
(J_{M_r}F)_k(U)
=
e^{-2\pi iM_r(v_r\cdot k)U}
\widehat F_k(-M_r(v_r\cdot k)).
\]

Using the source Diophantine bound

\[
|v_r\cdot k|\ge c/(1+|k|),
\]

one obtains, **across a fixed Gevrey radius gap**,

\[
\boxed{
\|A_ef\|_{G^s_{L_2}}
\le
C\exp[-cM_r^{1/(2s)}]
\|f\|_{G^s_{L_0}},
\qquad L_2<L_0.
}
\]

However the same note also proves a near-resonant obstruction: on the unrestricted one-radius Gevrey space there is no uniform estimate

\[
\|A_ef\|_{G^s_L}
\le\eta(M_r)\|f\|_{G^s_L},
\qquad \eta(M_r)\to0.
\]

Thus the earlier proposed one-space contraction with the compact radial remainder treated as a stretched-exponentially small same-space block is **not proved and in that unrestricted form is false**.

## Preferred radial repair — one-sided characteristic input-output inverse

`proofs/radial_characteristic_forward_inverse.md` introduces the straightened radial characteristic field

\[
\boxed{\mathcal T_r=\partial_U+M_rv_r\cdot\partial_y}
\]

and the exact one-sided inverse

\[
\boxed{
(\mathcal K_rF)(U,y)
=
\int_{U_-}^{U}
F(\sigma,y+M_r(U-\sigma)v_r)\,d\sigma.
}
\]

It satisfies

\[
\boxed{
\mathcal T_r\mathcal K_rF=F,
\qquad
\|\mathcal K_rF\|\le (U_+-U_-)\|F\|,
}
\]

with no torus derivative loss or Diophantine denominator.

The price is an outgoing radial trace rather than two-sided compact radial support. This matches the temporal input-output philosophy and is now the preferred exact-closure route.

## Aggregate non-radial small factor

The current block audit gives

\[
\boxed{
\eta_{\rm nr,\ell}
\le
C_MS_*^C
\left[
\varepsilon^{0.17}
+\varepsilon^{1/2-\kappa_s}
+\varepsilon^{0.9-2\kappa_s}
+\varepsilon^{1-2\kappa_s}
+\rho_\ell
\right].
}
\]

For the source parameter regime all displayed exponents are positive, hence

\[
\boxed{\eta_{\rm nr,\ell}\to0.}
\]

This is the main new structural result: **outside the radial compact-support mechanism, no visible nonlinear mean block remains with an unavoidable O(1) Lipschitz factor.**

## Immediate frontier — coupled temporal-radial characteristic mean theorem

The sharp remaining local target is now to reorganize the complete mean equations as a forward problem on a bounded temporal-radial collar:

\[
\boxed{
(m_{\rm in},z_{\rm in},r_{\rm in})
\longmapsto
(m_{\rm out},z_{\rm out},r_{\rm out}),
}
\]

where `r` is the finite collection of radial mean traces.

The theorem must prove in one fixed positive-radius characteristic Gevrey space that, after all linear mean terms are absorbed into the forward propagator,

\[
\boxed{
\|\mathcal N_{\rm mean}(m)-\mathcal N_{\rm mean}(\tilde m)\|
\le
\eta_\ell\|m-\tilde m\|,
\qquad
\eta_\ell\to0.
}
\]

The four remaining obligations are:

1. prove the quantitative exponent-preserving `m -> z(m)` difference estimate;
2. verify that all radial pressure/stress equations can simultaneously use the one-sided radial boundary formulation without a hidden global moment compatibility condition;
3. pin the signed-stress block in the same norm and replace the conservative `0.17` shorthand by the exact source exponent;
4. combine the bounded temporal, radial, moment, pressure/stress, and nonzero propagators into one coupled forward contraction.

If these close, Banach contraction gives the exact angular mean and therefore the **full local zero-force relay**.

## Global frontier after local mean closure

Only after the coupled mean theorem is closed should the project move decisively to physical-scale inheritance:

\[
(A_{c,\rm out},Z_{\rm out},r_{\rm out})\text{ at }q_j
\longrightarrow
\text{valid next-relay input at }q_{j+1}<q_j.
\]

## Not proved

- The quantitative same-norm exponent-preserving `m -> z(m)` difference estimate.
- Simultaneous one-sided radial pressure/stress closure of the full source mean equations.
- The coupled temporal-radial characteristic mean contraction theorem.
- Exact local zero-force residual closure of the complete field.
- Publication-final Controlled-Overlap Local Difference-Relay Theorem.
- Physical-scale inheritance from a generated child at `q_j` to a valid next-relay state at a smaller physical scale.
- Iterability through infinitely many physical scales.
- A finite or infinite autonomous Navier--Stokes relay chain producing singularity.
- Exact global closure `R(u,p)==0` on the complete space-time construction.
- Finite-time blowup for unforced 3D Navier--Stokes.

## Publication threshold

**Not crossed for an exact local zero-force theorem.**

The mean obstruction is now more sharply understood. The fast-time divisor is removed; the finite moment and visible nonlinear feedback blocks carry positive small factors; the radial Gevrey mechanism is quantitatively small only across a radius gap and has a genuine same-space near-resonant obstruction. The preferred next attack is the one-sided radial characteristic formulation plus the quantitative `m -> z(m)` Lipschitz estimate.