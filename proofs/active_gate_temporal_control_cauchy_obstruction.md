# Cauchy obstruction for temporally localized active-gate controls

**Status:** PROVED CLAIM-DISCIPLINE / AUTONOMY OBSTRUCTION TO THE CURRENT CONTROL INTERPRETATION. The finite-dimensional rank calculations in the corrected active gates remain valid as local response calculations, but the specific realization

\[
a_C(v;p)=a_C^{base}(v)+\sum_j p_j\chi_j(v)a_C^{ref}(v),
\qquad \chi_j\in C_c^\infty(I_v),
\]

cannot by itself be interpreted as a family of independently adjustable **autonomous unforced Cauchy controls**. On the actual physical graph the native pulse coordinate `v` is an affine function of physical time only (for fixed label/copy center). Thus the factors `chi_j(v)` are genuine spacetime/time-window modulations of the approximate packet, not independent spatial directions already encoded in one physical Cauchy trace.

The correct repair is to retain the finite response/rank mechanism but realize its columns by distinct incoming physical packet lobes whose amplitudes are present on a Cauchy slice and whose support trajectories enter the collision supernode at distinct times. That Cauchy-realizable lobe replacement is the next theorem obligation.

This note does **not** invalidate the principal polarization/action calculations or the finite response determinants. It invalidates the step from those determinants to an autonomous unforced forward gate unless the controls are re-realized from incoming Cauchy data.

## 1. Exact source identity for the native graph clock

The source formalization defines the physical native graph by

\[
Y_n(w)
=J_g^{i(n)}
\left(v_r\,r(w)^{d_r}+v_t\,w_t\right),
\]

where `w_t` is the physical time coordinate. The dual temporal coordinate satisfies the exact theorem

\[
\boxed{
\eta\bigl(Y_n(w)\bigr)
=T_g^{i(n)}w_t.
}
\tag{CO1}

This is `PhysicalGraphBounds.etaCoordinate_nativeGraph`.

The radial contribution vanishes because

\[
\eta(v_r)=0,
\qquad
\eta(v_t)=1.
\]

Hence the native temporal slot coordinate contains no physical spatial variable.

## 2. Exact physical slot time

For a fixed physical copy center `c`, the source defines

\[
\operatorname{slotTime}_{n,c}(w)
=
\frac{\eta(Y_n(w)-c)+r_0}
{c_n},
\]

where

\[
c_n
=T_g^{i(n)}Q_n^{1+h}
\]

is the source `timeCoefficient`.

Using (CO1),

\[
\boxed{
\operatorname{slotTime}_{n,c}(w)
=
\frac{T_g^{i(n)}w_t-\eta(c)+r_0}
{T_g^{i(n)}Q_n^{1+h}}.
}
\tag{CO2}

Therefore, for fixed label/copy center,

\[
\boxed{
\operatorname{slotTime}_{n,c}(w)
\text{ depends only on physical time }w_t.
}
\tag{CO3}

The physical source files prove this is not merely a native-coordinate model: `ActualSignedPhysicalData.slotMap_eq_native` identifies the actual Cartesian-lift slot map with the native slot coordinates exactly.

## 3. Consequence for the existing active controls

The active-gate realization introduced controls of the form

\[
\chi_j(v),
\qquad
\chi_j\in C_c^\infty(I_v),
\]

with distinct translated centers `tau_j` inside the collision collar.

After actual physical graph restriction, (CO2) gives

\[
\boxed{
\chi_j(v)
=
\chi_j\!\left(
\frac{T_g^{i(n)}w_t-\eta(c)+r_0}
{T_g^{i(n)}Q_n^{1+h}}
\right).
}
\tag{CO4}

Thus these controls are localized in **physical time**.

On a fixed physical Cauchy slice

\[
w_t=t_{in},
\]

every `chi_j(v)` is merely a scalar constant on the spatial variables of that fixed carrier. If `t_in` lies before all of their compact temporal supports, all control contributions vanish on that Cauchy slice.

Hence the four/nine/three independent parameters are not automatically independent spatial Cauchy directions.

## 4. Why this matters for an unforced forward solution

For smooth Navier--Stokes before blowup, a physical Cauchy state determines its future uniquely.

Therefore a family of exact unforced solutions with distinct gate outputs cannot be generated by changing only compactly supported **future time profiles** while leaving the same physical entrance trace unchanged.

The parameter-dependent local zero-residual theorem writes

\[
z(v)
=V_{p,m}(v,v_-)z_{in,p}+\int_{v_-}^v\cdots,
\]

so it formally allows a `p`-dependent incoming correction trace `z_{in,p}`. But the existing gate theorem does not identify that trace as a renewed physical Cauchy observable, nor does it prove that the temporal controls are generated from that trace by autonomous forward evolution.

Consequently the implication

\[
\boxed{
\text{time-localized approximate control family}
\Longrightarrow
\text{autonomous unforced Cauchy gate}
}
\]

has not been proved.

## 5. What remains valid

The following local calculations remain useful and unchanged:

1. exact action resonances;
2. finite promoted-block enumeration;
3. principal polarization nonvanishing;
4. frozen controllability/PBH/Vandermonde ranks;
5. stability of those ranks under small coefficient perturbations;
6. exact correction theory for any **genuinely supplied incoming physical finite-dimensional family** in the same audited source classes.

Thus the rank calculations should be interpreted as specifying how many independent **incoming physical packet directions** are required, not as proving that future temporal cutoff amplitudes can be freely chosen in an autonomous solution.

## 6. Correct repair target

Replace one temporally modulated carrier

\[
C_{base}+\sum_jp_j\chi_j(v)C_{ref}
\]

by a finite family of physically distinct incoming unit-beta lobes

\[
\boxed{C_1,\ldots,C_N}
\tag{CO5}

whose complex amplitudes are already encoded in the physical Cauchy trace at the beginning of the stage.

Use support/transport geometry so that the `j`th lobe meets the relevant partner only in a short collision interval centered at `tau_j`. Along that collision trajectory its overlap source has the same translated-bump response profile used in the finite rank calculation.

Then the control parameters are genuine Cauchy amplitudes:

\[
\boxed{
p_j=A(C_j)|_{t=t_{in}}.
}
\tag{CO6}

Distinct response columns arise from distinct collision times/support trajectories, not from externally choosing a future time modulation.

The already-developed nine-lobe unit-beta bank is precisely the finite carrier architecture in which such a replacement can live.

## 7. New sharp local frontier

The next theorem must prove a **Cauchy-realizable translated-lobe control lemma**:

> Given a finite set of desired collision centers inside one source-safe collar, construct support-separated incoming physical unit-beta lobes on an earlier physical Cauchy slice such that their forward homogeneous/designated transport produces the required collision-overlap profiles, while all non-designated overlaps are absent or action-subcritical.

After that lemma, the existing four-, nine-, and three-column rank arguments can be rerun with the lobe amplitudes as honest incoming Cauchy coordinates.

Until this repair is complete, the active local cells should not be cited as autonomous unforced Cauchy gates, and no global unforced cascade/blowup theorem is claimed.
