# Exact discrete finite-`S` tangent-Gaussian central profile

**Status:** PROVED DISCRETE CENTRAL PROFILE FROM THE ESTABLISHED `O(h_S^2)` NORMAL FORM / ONE NORMALIZATION PER SECTOR / FULL CORE+TAIL PDE FIXED POINT STILL OPEN.

This note closes the central finite-`S` profile obligation left after
`beta21_lowu_finiteS_profile_normal_form.md` and the compact-bank obstruction.

The key point is that the tangent-normalized one-cell operator is not inverted as an `O(S) x O(S)` matrix.  Instead one uses the already established singular expansion

\[
\mathcal P_{b,S}^{tan}
=I+h_S\mathcal L_b+h_S^2\mathcal K_{b,S},
\qquad h_S=S^{-1/2},
\tag{DP1}
\]

on the central Gaussian graph space, where

\[
\boxed{
\mathcal L_b
=v_b\partial_y+B_by+C_b,
}
\tag{DP2}
\]

with `v_b != 0` in both sectors and where the remainder operators `K_{b,S}` are uniformly bounded at the fixed graph order used in the normal-form theorem.

The operator `L_b` has a one-dimensional kernel generated by the explicit continuum profile.  After fixing one normalization, `L_b` is Volterra-invertible on the complementary codimension-one space.  Since the inverse of `h_S L_b` costs only `h_S^{-1}` while the discrete remainder is `h_S^2`, the perturbation size is `O(h_S)`.  A Neumann argument therefore produces an exact discrete fixed profile

\[
\boxed{
\mathcal P_{b,S}^{tan}f_{b,S}=f_{b,S}
}
\tag{DP3}
\]

with

\[
\boxed{
f_{b,S}=f_b+O(S^{-1/2})}
\tag{DP4}
\]

in the physical tangent-Gaussian graph norm.

This theorem concerns the **central profile sector**.  It does not contradict the compact-support obstruction: the exact full reset state still requires the analytic correction tail outside the compact primary bank.

## 1. Established normal form

For each sector `b in {C,P}`, write

\[
A_{b,S}:=\mathcal P_{b,S}^{tan}-I.
\]

The finite-`S` normal-form theorem gives

\[
\boxed{
A_{b,S}
=h_S\mathcal L_b+h_S^2\mathcal K_{b,S},
}
\tag{DP5}
\]

where

\[
h_S=S^{-1/2}
\]

and

\[
\sup_{S\ge S_0}
\|\mathcal K_{b,S}\|_{X_b^{m+2}\to X_b^m}
\le C_m.
\tag{DP6}
\]

Here `X_b^m` is the physical tangent-Gaussian graph space: the coefficient profile is measured after multiplication by the physical large-deviation Gaussian.  The localization audit proves that the continuum kernel profile lies in this space with a strict Gaussian margin.

The constants are

\[
\boxed{
v_C=-2.046933015584520\ldots\ne0,}
\]

\[
\boxed{
v_P=-0.185170992872966
-1.044351775683794\,i\ne0.}
\]

The Gaussian drift coefficients are

\[
B_C=1.509777659068068\ldots,
\]

\[
B_P=-4.618831967884313
+2.657928294237038\,i.
\]

A constant `C_b` records the finite-`S` centering and macroscopic parameter drift.  It translates the profile center but does not alter the one-dimensional kernel structure or the strict localization margin.

## 2. Exact continuum kernel

The equation

\[
\mathcal L_bf=0
\]

has the explicit solution

\[
\boxed{
\phi_b(y)
=
\exp\left(
-\frac{B_b}{2v_b}y^2
-\frac{C_b}{v_b}y
\right).
}
\tag{DP7}
\]

Thus

\[
\ker\mathcal L_b
=\operatorname{span}\{\phi_b\}.
\tag{DP8}
\]

The physical localization theorem gives

\[
\phi_b\in X_b^{m+2}
\]

with a strict Gaussian reserve: the mild anti-Gaussian coefficient drift is dominated by the physical action curvature.

Fix a bounded linear normalization functional

\[
\ell_b:X_b^m\to\mathbb C
\]

such that

\[
\boxed{\ell_b(\phi_b)=1.}
\tag{DP9}
\]

For example one may use evaluation at the profile center after fixing the common phase, or a Gaussian-weighted pairing supported in a compact central interval.

Define

\[
X_{b,0}^m
:=\{g\in X_b^m:\ell_b(g)=0\}.
\tag{DP10}
\]

## 3. Volterra right inverse for `L_b`

For

\[
\mathcal L_bu=g,
\]

variation of constants gives

\[
\boxed{
 u(y)
=\phi_b(y)
\left[
 c
+\frac1{v_b}
\int_{y_*}^{y}
\phi_b(s)^{-1}g(s)\,ds
\right].
}
\tag{DP11}
\]

The integration orientation is chosen separately on the two Gaussian tails so that the physical weighted solution remains in `X_b^{m+1}`; the two pieces differ by a multiple of `phi_b`, which is then fixed by the normalization `ell_b(u)=0`.

Because the physical Gaussian exponent has a **strict** positive localization reserve, the weighted Hardy/Volterra estimate yields

\[
\boxed{
\|G_b g\|_{X_b^{m+1}}
\le C_m\|g\|_{X_b^m},
}
\tag{DP12}
\]

where

\[
G_b:X_{b,0}^m\to X_{b,0}^{m+1}
\]

is the normalized inverse and

\[
\boxed{
\mathcal L_bG_b=I
\quad\text{on }X_{b,0}^m.
}
\tag{DP13}
\]

No `S` appears in (DP12).

## 4. Rescaled inverse and perturbation size

On the normalized complement,

\[
(h_S\mathcal L_b)^{-1}
=h_S^{-1}G_b.
\]

Therefore

\[
\boxed{
\|(h_S\mathcal L_b)^{-1}\|
\le C_mh_S^{-1}
=C_m\sqrt S.
}
\tag{DP14}
\]

Apply this inverse to the remainder in (DP5):

\[
(h_S\mathcal L_b)^{-1}
(h_S^2\mathcal K_{b,S})
=h_SG_b\mathcal K_{b,S}.
\]

Using (DP6) and (DP12),

\[
\boxed{
\|h_SG_b\mathcal K_{b,S}\|
\le C_mh_S
=C_mS^{-1/2}.
}
\tag{DP15}
\]

Hence for all sufficiently large `S`,

\[
I+h_SG_b\mathcal K_{b,S}
\]

is invertible by a Neumann series on the normalized complement.

## 5. Exact normalized discrete kernel

We seek a fixed profile in the form

\[
f_{b,S}=\phi_b+w_{b,S},
\qquad
\ell_b(w_{b,S})=0.
\tag{DP16}
\]

The exact fixed equation is

\[
A_{b,S}(\phi_b+w_{b,S})=0.
\]

Since

\[
\mathcal L_b\phi_b=0,
\]

( DP5 ) gives

\[
\mathcal L_bw_{b,S}
=-h_S\mathcal K_{b,S}
(\phi_b+w_{b,S}).
\tag{DP17}
\]

Applying `G_b` yields

\[
\boxed{
 w_{b,S}
=-h_S
(I+h_SG_b\mathcal K_{b,S})^{-1}
G_b\mathcal K_{b,S}\phi_b.
}
\tag{DP18}
\]

Consequently

\[
\boxed{
\|w_{b,S}\|_{X_b^{m+1}}
\le C_mh_S
=C_mS^{-1/2}.
}
\tag{DP19}
\]

Thus the normalized discrete profile exists and is unique near the continuum tangent-Gaussian profile:

\[
\boxed{
 f_{b,S}
=\phi_b+O(S^{-1/2}).
}
\tag{DP20}
\]

Substituting into (DP5),

\[
\boxed{
\mathcal P_{b,S}^{tan}f_{b,S}
=f_{b,S}.
}
\tag{DP21}
\]

## 6. Catalyst and parent simultaneously

The construction is sectorwise.  The catalyst and parent central profiles use the same physical beta-zero Cauchy root but have distinct tangent-normalized profile spaces and distinct operators `L_C,L_P`.

The single-root coupling theorem already proves that one scalar root amplitude and one transverse polarization parameter provide the required two principal shift strengths.  The source growing-coordinate normalization theorem removes the apparent hidden sector factor.

Therefore the two normalized discrete profile solves do not introduce two new physical root controls.  Their normalization constants represent the overall profile amplitudes/phases already present in the designated Cauchy data.

The macroscopic parameters `(kappa,theta,A_M,tau)` remain available for the finite-dimensional reset/phase conditions exactly as in the corrected parameter ledger.

## 7. Relation to the compact-bank obstruction

Equation (DP21) is an exact fixed equation in the **tangent-Gaussian central profile sector**, not an assertion that the finitely supported primary packet bank by itself is fixed.

At finite `S`, the physical primary bank must still be cut off to source support.  That cutoff produces the exponentially small defect

\[
\|d_{bd,S}\|
\le S^Ae^{-cS}.
\]

The theorem
`beta21_lowu_finiteS_tail_parametrix_and_secondary_resonance.md`
constructs a polynomially bounded analytic-tail right inverse for precisely that defect.

Thus the corrected local architecture is now

\[
\boxed{
\text{exact discrete tangent-Gaussian core}
+\text{ analytic tail completion}.
}
\tag{DP22}

No contradiction with

\[
\ker_{c_{00}}(e^{\lambda R}-R^m)=\{0\}
\]

occurs because the exact full state is not finitely supported.

## 8. Parameter derivatives

The coefficients in `L_b`, the continuum kernel `phi_b`, and the bounded remainder `K_{b,S}` depend smoothly on the finite macroscopic parameter vector in a fixed neighborhood of the low-`u` point.

Differentiating (DP18) at a fixed finite parameter order gives only:

- fixed powers of `S` from source coefficient derivatives;
- the normalized inverse bound `O(sqrt(S))` from (DP14);
- one explicit factor `h_S` in (DP18).

Hence the exact discrete profiles have polynomial `C^1` parameter bounds compatible with the source-small PDE correction and the final finite-dimensional implicit-function step.

## 9. Updated Gate 1 status

The previous central missing theorem is now reduced to already established ingredients and closed:

\[
\boxed{
\text{exact finite-}S\text{ tangent-Gaussian central profiles exist}
\atop
\text{and converge to the continuum profiles at rate }O(S^{-1/2}).
}
\tag{DP23}

What remains before a complete Gate 1 theorem is no longer the central designated profile and no longer the tail spectral inverse.  It is the **coupled gluing theorem**:

1. cut the exact central profiles to the source-supported primary bank;
2. solve the boundary defect in the analytic tail using the polynomial parametrix;
3. simultaneously re-run the existing mean/nonzero zero-residual Banach solve;
4. transfer the source-small correction to the finite macroscopic reset observables;
5. apply the already proved transversality/IFT.

All infinite-dimensional inverse losses in this list are now at most polynomial in `S`, while every forcing defect is source-small or exponentially small.

No global cascade or blow-up theorem is claimed here.
