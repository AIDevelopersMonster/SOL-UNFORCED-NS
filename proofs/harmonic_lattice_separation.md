# Harmonic-lattice separation for the v0.4 difference relay

**Status:** PROVED PRINCIPAL GEOMETRIC LEMMA; HIGH-MODE DAMPING REDUCTION DERIVED. The lattice separation is exact for the reference phase normals on the certified relay bracket. Passing the high-mode statement to the full localized coefficient system still requires the source-class perturbation audit.

## 1. Two parent generators

Work in the reference `(e_r,K)` plane and write the two parent phase normals, after removing the common positive factor `B_s`, as

\[
v_1=\beta_1(x_1,1),
\qquad
v_2=\beta_2(y,1),
\]

with the current v0.4 data

\[
\beta_1=\frac{25}{16},
\qquad
\beta_2=\frac9{16},
\qquad
x_1=\frac{29}{32},
\]

and the certified catalyst bracket

\[
\frac{613}{500}\le y\le\frac{1227}{1000}.
\]

Every phase generated by repeated products of the two real parent packets belongs to the integer lattice

\[
\Lambda=\{a\Phi_1+b\Phi_2:(a,b)\in\mathbb Z^2\}.
\]

Its reference spatial normal is

\[
n_{a,b}^{(0)}=B_s(av_1+bv_2).
\]

## 2. Uniform nondegeneracy of the two generators

Let `A(y)` be the `2 x 2` matrix with columns `v_1,v_2`. Then

\[
|\det A(y)|
=\beta_1\beta_2(y-x_1).
\]

On the certified bracket,

\[
y-x_1
\ge
\frac{613}{500}-\frac{29}{32}
=
\frac{1279}{4000}.
\]

Since

\[
\beta_1\beta_2=\frac{225}{256},
\]

we obtain

\[
|\det A(y)|
\ge
\frac{287775}{1024000}
>
\frac7{25}.
\tag{H1}
\]

A direct rational estimate at the upper endpoint gives

\[
\|A(y)\|_F<\frac{23}{10}.
\tag{H2}
\]

For a `2 x 2` matrix,

\[
\sigma_{\min}(A)
=\frac{|\det A|}{\sigma_{\max}(A)}
\ge
\frac{|\det A|}{\|A\|_F}.
\]

Combining (H1) and (H2),

\[
\sigma_{\min}(A)
>
\frac{14}{115}
>
\frac3{25}.
\]

Therefore for every real pair `(a,b)`, and hence in particular for every integer pair,

\[
\boxed{
|av_1+bv_2|
\ge
\frac3{25}\sqrt{a^2+b^2}.
}
\tag{H3}
\]

Restoring `B_s`,

\[
\boxed{
|n_{a,b}^{(0)}|
\ge
\frac3{25}B_s\sqrt{a^2+b^2}.
}
\tag{H4}
\]

Thus the only zero vector in the reference phase lattice is the genuine mean mode `(a,b)=(0,0)`. There is no sequence of nonzero integer combinations whose reference carrier tends to zero.

## 3. First correction generation

Use the lattice indices

\[
e_1=(1,0),
\qquad
e_2=(0,1),
\qquad d=e_1-e_2=(1,-1),
\qquad s=e_1+e_2=(1,1).
\]

Here `d` is the desired child and `s` is the first unwanted sum correction.

Interactions of `s` with the primary set `{e_1,e_2,d}` generate only

\[
\begin{array}{c|c}
\text{interaction} & \text{lattice index}\\ \hline
s-e_1 & e_2\\
s-e_2 & e_1\\
s+d & 2e_1\\
s-d & 2e_2\\
s+e_1 & (2,1)\\
s+e_2 & (1,2)\\
s+s & (2,2)\\
s-s & (0,0).
\end{array}
\tag{H5}
\]

Hence the first stable correction creates four types of output only:

1. **resonant returns** to the two parent phases;
2. second harmonics of a parent;
3. higher nonzero lattice modes;
4. the genuine mean block.

It creates no new independent phase direction and no new irrational small-divisor problem.

The resonant returns are at least cubic in the tail amplitudes because the sum correction itself is quadratic in the parents. The mean block must be treated by the mean/compatibility inverse rather than by oscillatory damping.

## 4. High lattice modes are increasingly viscous

For a reference nonzero lattice mode, its principal viscous coefficient is

\[
d_{a,b}
=\varepsilon k^2|n_{a,b}^{(0)}|^2.
\]

The source normalization gives

\[
\varepsilon k^2B_s^2
=
\frac{\lambda_0}{(1+u_*^2)^{3/2}}.
\]

Using (H4),

\[
\boxed{
d_{a,b}
\ge
\frac9{625}
\frac{\lambda_0}{(1+u_*^2)^{3/2}}
(a^2+b^2).
}
\tag{H6}
\]

The projected inviscid symbol is homogeneous of degree zero in the phase normal. On the fixed relay rectangle, lattice nondegeneracy (H3) therefore gives a uniform bound

\[
\|A_{a,b}^{\rm inv}\|
\le C_{\rm sh}\lambda_0
\tag{H7}
\]

for a constant depending only on the fixed background/profile compact set, not on `(a,b)`.

Consequently every lattice mode satisfying

\[
\boxed{
a^2+b^2
>
\frac{625}{9}C_{\rm sh}(1+u_*^2)^{3/2}
}
\tag{H8}
\]

has viscous damping larger than the inviscid growth bound. With a factor two inserted on the right of (H8), one obtains a fixed positive stable gap.

Thus **only finitely many low lattice modes can require individual resonance analysis**. The infinite harmonic tail is automatically placed in a viscously stable sector.

## 5. Perturbation to actual source-type normals

The beta-phase audit gives parent normal errors of size `O(S_*^{-1})`. For a lattice index `(a,b)`, linear combination gives an error bounded by

\[
CS_*^{-1}(|a|+|b|)
\le C\sqrt2S_*^{-1}\sqrt{a^2+b^2}.
\]

Because the reference lower bound in (H3) is itself linear in `sqrt(a^2+b^2)`, one sufficiently large level preserves a uniform fraction of the lattice gap simultaneously for **all** lattice indices, provided the common lower bound for `B_s` on the relay compact set is used.

This is important: the phase-normal perturbation does not accumulate into a small divisor at large harmonic index.

## 6. Consequence for the zero-force correction program

The correction problem decomposes into three blocks:

\[
\boxed{
\text{finite low nonzero lattice block}
\oplus
\text{mean block}
\oplus
\text{uniformly stable high lattice tail}.
}
\tag{H9}
\]

The first unwanted sum mode `s=(1,1)` already has a quantitative stable inverse. Formula (H5) identifies the first resonant returns explicitly. Formula (H8) shows that an infinite cascade of ever more delicate near-resonances cannot occur at large lattice index.

What remains is to build a **stage-uniform right inverse** on the finite low block plus mean compatibility block and combine it with the stable high-mode inverse in a Banach norm that is closed under quadratic convolution.
