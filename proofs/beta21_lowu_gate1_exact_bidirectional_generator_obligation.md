# Gate 1B: exact finite-\(S\) bidirectional designated generator obligation

**Status:** REDUCTION / EXACT SOURCE-FRAME COEFFICIENT THEOREM STILL OPEN.

This note records the corrected finite-\(S\) designated operator after the real-root conjugate-sideband repair.

The designated root is real and therefore contains both \(+M\) and \(-M\).  Hence the finite-\(S\) beta-one/beta-two orbit generator must be tridiagonal in orbit index.

No exact discrete fixed profile is claimed here.

---

## 1. Exact orbit indexing

Use

\[
C_j=C+jM,\qquad Q_n=P+nM.
\]

Then

\[
(+M)+C_j=C_{j+1},\qquad (-M)+C_j=C_{j-1},
\]

and similarly

\[
(+M)+Q_n=Q_{n+1},\qquad (-M)+Q_n=Q_{n-1}.
\]

Thus the real-root principal interaction preserves each beta sector and couples nearest neighbors in both directions.

---

## 2. Finite-\(S\) designated generator

For one sector \(b\in\{1,2\}\), let \(a_j(v)\) denote the exact source/action-normalized designated orbit coefficient.

The exact generator must have the form

\[
\boxed{
(\mathcal G_{b,S}(v)a)_j
=
d_{b,j,S}(v)a_j
+
p_{b,j,S}(v)a_{j-1}
+
q_{b,j,S}(v)a_{j+1},
}
\tag{BG1}
\]

where:

- \(d_{b,j,S}\) contains the diagonal pulse/viscous/frame rate after the chosen action normalization;
- \(p_{b,j,S}\) is the exact \(+M\) coupling into index \(j\);
- \(q_{b,j,S}\) is the exact \(-M\) coupling into index \(j\).

The exact orientation of \(p,q\) is a convention; both must be retained.

The one-cell propagator is the time-ordered exponential

\[
\boxed{
\mathcal U_{b,S}
=
\mathcal T
\exp\left(
\int_{v_-}^{v_+}\mathcal G_{b,S}(v)\,dv
\right).
}
\tag{BG2}
\]

Reset relabelling gives

\[
\boxed{
\mathcal P_{b,S}
=
R^{-m_b}\mathcal U_{b,S},
\qquad
m_1=1,\quad m_2=2.
}
\tag{BG3}
\]

The exact discrete profile problem is

\[
\boxed{
\mathcal P_{b,S}f_{b,S}=f_{b,S}.
}
\tag{BG4}
\]

---

## 3. Limiting coefficients

At the corrected principal point from
beta21_lowu_real_root_bidirectional_principal_reset.md,

\[
\tau=-5,
\]

\[
\kappa_\diamond
=
0.6104830846712222\ldots,
\]

\[
A_\diamond
=
0.05317726570772321\ldots,
\]

\[
\theta_\diamond=\pi,
\qquad
\phi_{M,\diamond}=0.
\]

Define

\[
c_{b,+}=\frac12+b\tau,
\qquad
c_{b,-}=-\frac12+b\tau.
\]

Then the translation-invariant limiting generator is

\[
\boxed{
\mathcal G_b^{(0)}
=
A_\diamond
\left(
c_{b,+}R+c_{b,-}R^{-1}
\right).
}
\tag{BG5}
\]

The limiting Poincare map is

\[
\boxed{
\mathcal P_b^{(0)}
=
R^{-m_b}
\exp\left[
A_\diamond
\left(
c_{b,+}R+c_{b,-}R^{-1}
\right)
\right].
}
\tag{BG6}
\]

Its geometric fixed ratios are

\[
\rho_{C,\diamond}
=
0.4892524446551738\ldots,
\]

\[
\rho_{P,\diamond}
=
-1.8582019557361078\ldots.
\]

---

## 4. Exact source coefficient target

The required source theorem is not merely

\[
p_{b,j,S}=p_b^{(0)}+O(S^{-1}).
\]

Because the central profile occupies

\[
|j|=O(\sqrt S),
\]

the needed statement is a profile-scale \(C^2\) expansion.

Write

\[
y=\frac j{\sqrt S},
\qquad
h=S^{-1/2}.
\]

Then uniformly for \(|y|\le S^\eta\) with one fixed small \(\eta>0\),

\[
\boxed{
d_{b,j,S}(v)
=
d_b^{(0)}(v)
+
h\,d_b^{(1)}(v)y
+
h\,d_b^{(c)}(v)
+
O\!\left(h^2(1+y^2)\right),
}
\tag{BG7}
\]

\[
\boxed{
p_{b,j,S}(v)
=
p_b^{(0)}(v)
+
h\,p_b^{(1)}(v)y
+
h\,p_b^{(c)}(v)
+
O\!\left(h^2(1+y^2)\right),
}
\tag{BG8}
\]

\[
\boxed{
q_{b,j,S}(v)
=
q_b^{(0)}(v)
+
h\,q_b^{(1)}(v)y
+
h\,q_b^{(c)}(v)
+
O\!\left(h^2(1+y^2)\right).
}
\tag{BG9}
\]

The remainders must hold in the exact fixed physical/source packet norm selected for Gate 1E, including first parameter derivatives.

This is the true finite-\(S\) normal-form input.

---

## 5. Why tridiagonal structure is better than the old derivative expansion

The old continuum Taylor argument expanded \(R^{\pm1}\) into powers of \(\partial_y\) and then tried to invert a first-order operator with a second-order perturbation.

That creates a derivative-loss singular perturbation.

The exact discrete problem (BG4) does not require this expansion.

The finite-\(S\) generator itself is nearest-neighbor in index, while the propagator has exponentially/factorially decaying long-range kernel generated by repeated nearest-neighbor interactions.

Thus the correct exact analysis should work in a weighted sequence space and use:

- tridiagonal generator bounds;
- transfer matrices / exponential dichotomy;
- comparison with the translation-invariant bilateral operator;
- Gaussian action weights.

The continuum differential operator is diagnostic, not the primary inverse.

---

## 6. Tangent-normalized exact sequence space

Factor the geometric ratio:

\[
a_j=\rho_b^j f_j.
\]

Define the tangent-normalized weighted sequence norm

\[
\boxed{
\|f\|_{\mathcal X_{b,S}}^2
=
\sum_j
|f_j|^2
\exp\left(
2S H_b(z_{b,j})
\right)
\omega_{b,S}(j),
}
\tag{BG10}
\]

where \(\omega_{b,S}\) is one fixed polynomial/Gaussian graph weight sufficient for the finite derivative/output ledger.

The physical action satisfies

\[
H_b(x)=H_b'(x)=0,
\qquad
H_b''(x)<0.
\]

Hence on the central window

\[
S H_b(z_{b,j})
=
-\alpha_bd_b^2\frac{j^2}{S}
+
O\left(\frac{|j|^3}{S^2}\right).
\]

Thus \(\mathcal X_{b,S}\) is a discrete Gaussian space concentrated on

\[
|j|=O(\sqrt S).
\]

---

## 7. Exact discrete theorem target

For each \(b=1,2\), Gate 1B/C should prove:

### Existence

There exists nonzero

\[
f_{b,S}\in\mathcal X_{b,S}
\]

such that

\[
\mathcal P_{b,S}f_{b,S}=f_{b,S}.
\]

### Normalization

One explicit bounded scalar functional satisfies

\[
\ell_{b,S}(f_{b,S})=1.
\]

### Uniqueness

The normalized admissible fixed profile is unique in a fixed neighborhood of the limiting Gaussian profile.

### Convergence

After interpolation \(y=j/\sqrt S\),

\[
\boxed{
I_S f_{b,S}
\to
f_{b,\diamond}
}
\tag{BG11}
\]

in one fixed Gaussian profile norm.

### Parameter regularity

The profile depends \(C^1\) on

\[
(\kappa,A,\theta,\phi_M)
\]

near the corrected principal point, with fixed polynomial \(S\)-loss at most.

---

## 8. Candidate dichotomy mechanism

After tangent normalization, the limiting operator is generated by

\[
\widetilde{\mathcal G}_b^{(0)}
=
A_\diamond
\left(
c_{b,+}\rho_b R
+
c_{b,-}\rho_b^{-1}R^{-1}
\right),
\]

followed by reset \(R^{-m_b}\).

The derivative of the limiting dispersion function with respect to spectral variable \(\rho\) is nonzero at the corrected root because

\[
v_b
=
A_\diamond
\left(
c_{b,+}\rho_b
-
c_{b,-}\rho_b^{-1}
\right)
-
m_b
\ne0.
\]

Numerically,

\[
v_C\approx-0.5192773,
\qquad
v_P\approx-1.3617508.
\]

This is the discrete analogue of simple-root transversality.

The preferred theorem is therefore a **simple spectral root plus slowly varying Jacobi-type coefficients implies one admissible discrete Gaussian branch**.

That theorem has not yet been written.

---

## 9. Boundary tails

At the corrected \(\kappa_\diamond\), the index span of the same strict physical slope core increases relative to the old \(\kappa_*\), but the physical action boundary values \(H_b(x\pm0.03)\) do not change because they depend only on \(u,x\).

Thus the boundary truncation still obeys

\[
\boxed{
\|d_{bd,S}\|
\le
S^Ae^{-c_{bd}S}
}
\tag{BG12}
\]

with the same physical \(c_{bd}>0\).

The analytic lattice price must be recomputed with the new index-density constants; the preliminary audit in the corrected principal theorem shows \(\sigma_0=0.005\) still has positive reserve.

---

## 10. Current exact frontier

The next source-level calculation is now unambiguous:

1. derive the exact \(+M\) coefficient \(p_{b,j,S}(v)\);
2. derive the exact conjugate \(-M\) coefficient \(q_{b,j,S}(v)\);
3. derive the diagonal coefficient \(d_{b,j,S}(v)\);
4. prove the profile-scale expansions (BG7)--(BG9);
5. only then attack the exact discrete fixed profile (BG4).

Until these steps are completed, no file should state that the full finite-\(S\) designated orbit is exactly reset.
