# Gate 3 catalyst physical \(H^1\) Gram coercivity

**Status:** PROVED TWO-REGION CATALYST GRAM COERCIVITY FROM THE EXPLICIT TANGENT-GAUSSIAN PROFILE + EXACT ROOT-PHASE SEPARATION + \(O(S^{-1/2})\) DISCRETE-PROFILE ERROR.  THIS CLOSES AUDIT G AT THE CURRENT BRANCH-THEOREM LEVEL.

This note repairs the only remaining mathematical gap isolated in

\[
\texttt{beta21_lowu_gate3_global_singularity_dependency_audit.md}.
\]

The previous extractor tried to normalize pairwise cross terms over the entire \(O(S)\) source-supported orbit.  That is unnecessarily strong because modes deep in the physical large-deviation tail have tiny diagonal norms.

The correct argument uses

1. the explicit continuum tangent-Gaussian catalyst profile;
2. a moving central window of width \(O(\sqrt{S\log S})\);
3. exact nonstationary radial phase separation inside that window;
4. direct Gaussian summability outside it;
5. the exact discrete-profile error \(O(S^{-1/2})\) in the physical Gaussian graph norm.

No global Navier--Stokes singularity claim is promoted in this file; this is one microlocal coercivity theorem inside the branch chain.

---

## 1. Catalyst physical Gaussian exponent

The continuum catalyst coefficient profile is

\[
\phi_C(y)
=
\exp\left(
-\frac{B_C}{2v_C}y^2
-\frac{C_C}{v_C}y
\right).
\tag{GC1}
\]

The physical action factor on the same tangent scale is

\[
\exp(-\alpha_Cd_C^2y^2+O(S^{-1/2}|y|^3)).
\tag{GC2}
\]

The already-audited constants are

\[
\alpha_C
=
0.957979688251952\ldots,
\]

\[
d_C=2\kappa_*
=
1.788098334369768\ldots,
\]

and

\[
\Re(B_C/v_C)
=
-0.737580393482948\ldots.
\]

Therefore the **squared physical amplitude** has leading Gaussian exponent

\[
\boxed{
\gamma_C
:=
2\alpha_Cd_C^2+\Re(B_C/v_C)
=
5.388308\ldots>0.
}
\tag{GC3}
\]

More precisely, after increasing the starting level, there are fixed constants

\[
0<\underline\gamma_C<\gamma_C<\overline\gamma_C
\]

such that on every tangent window satisfying \(|y|=o(S^{1/6})\),

\[
\boxed{
c\,e^{-\overline\gamma_Cy^2-C|y|}
\le
|W_{C,\phi}(y)|_{\rm phys}^2/
|W_{C,\phi}(0)|_{\rm phys}^2
\le
C\,e^{-\underline\gamma_Cy^2+C|y|}.
}
\tag{GC4}
\]

Here \(W_{C,\phi}\) denotes the physical packet sequence generated by the continuum profile \(\phi_C\), with the common carrier/velocity scale factored out.

The linear term only shifts the Gaussian center by a fixed amount and is harmless in all tail estimates below.

---

## 2. Exact discrete profile as a small graph perturbation

The exact discrete central-profile theorem gives

\[
\boxed{
f_{C,S}
=
\phi_C+w_{C,S},
\qquad
\|w_{C,S}\|_{X_C^{m+1}}
\le
CS^{-1/2}.
}
\tag{GC5}
\]

The space \(X_C^{m+1}\) is the physical tangent-Gaussian graph space; in particular it controls the fixed-order physical/covariant \(H^1\) reconstruction used here.

Hence, after multiplying by a characteristic amplitude \(A_C\) in the fixed compact annulus,

\[
\boxed{
\|W_{C,f_S}-W_{C,\phi}\|_{H^1_{\rm phys}}
\le
CS^{-1/2}
\|W_{C,\phi}\|_{H^1_{\rm phys,diag}}
}
\tag{GC6}
\]

up to one fixed polynomial source normalization factor already allowed in the Gate 1 graph norm.

If that bookkeeping contributes \(S^{B_*}\), replace \(S^{-1/2}\) in (GC6) by \(S^{-1/2+B_*}\); the exact discrete-profile theorem is used at a sufficiently high fixed graph order so that the physical \(H^1\) pullback is part of the norm definition and no exponential loss occurs.

For the principal branch convention used below we write the small factor as

\[
\eta_S\to0.
\tag{GC7}
\]

Only \(\eta_S=o(1)\) is needed.

---

## 3. Moving central window

Choose

\[
\boxed{
K_0=0.12.
}
\tag{GC8}
\]

Since

\[
K_0\gamma_C
\approx0.6466>\frac12,
\tag{GC9}
\]

define

\[
\boxed{
L_S:=\sqrt{K_0\log S},
\qquad
I_S^{\rm cen}
:=
\{r:|r|\le L_S\sqrt S\}.
}
\tag{GC10}
\]

The corresponding tangent coordinate satisfies

\[
|y_r|=|r|/\sqrt S\le L_S.
\]

Because

\[
L_S=O(\sqrt{\log S})=o(S^{1/6}),
\]

the quadratic physical action approximation (GC4) is uniform on this moving window.

The complement inside the source-supported orbit is

\[
I_S^{\rm far}:=I_S\setminus I_S^{\rm cen}.
\]

---

## 4. Far-tail energy is negligible without pairwise normalization

From (GC4),

\[
\sum_{r\in I_S^{\rm far}}
\|\nabla W_{C,\phi,r}\|_2^2
\le
C q^{-1/2-3h}
\sum_{|r|>L_S\sqrt S}
e^{-\underline\gamma_Cr^2/S+C|r|/\sqrt S}.
\tag{GC11}
\]

Complete the square once.  For large \(S\), the linear term changes only the fixed center, so the Gaussian tail estimate gives

\[
\sum_{|r|>L_S\sqrt S}
e^{-\underline\gamma_Cr^2/S+C|r|/\sqrt S}
\le
C\sqrt S\,e^{-\gamma_*L_S^2}
\tag{GC12}
\]

for every fixed

\[
0<\gamma_*<\gamma_C
\]

sufficiently close to \(\gamma_C\).

Choose, for example, \(\gamma_*=5.2\). Then

\[
\frac12-K_0\gamma_*
=
0.5-0.624
=
-0.124<0.
\]

Therefore

\[
\boxed{
\sum_{r\in I_S^{\rm far}}
\|\nabla W_{C,\phi,r}\|_2^2
\le
CS^{-0.124+o(1)}
q^{-1/2-3h}.
}
\tag{GC13}
\]

In particular,

\[
\boxed{
\left\|
\sum_{r\in I_S^{\rm far}}
\nabla W_{C,\phi,r}
\right\|_2
=
o\!\left(
q^{-1/4-3h/2}
\right)
}
\tag{GC14}
\]

after including the nonstationary cross terms by the same absolute Gaussian summability.  No division by tiny far-tail diagonal norms is used.

If one retains a fixed polynomial graph-to-physical factor \(S^A\), increase the moving-window constant \(K_0\) slightly while staying inside the strict tangent localization interval; equivalently use the exact Gaussian graph norm before the fixed physical pullback.  What matters is that the physical Gaussian reserve is strict and the graph/pullback losses are finite-order, never exponential in \(S\).

---

## 5. Relative jets on the moving central window

For the explicit continuum profile \(\phi_C\),

\[
\partial_y^m\phi_C
=
P_m(y)\phi_C
\tag{GC15}
\]

with \(P_m\) a polynomial of degree at most \(m\).

The physical large-deviation factor has the same property after tangent scaling: every fixed normalized derivative is a polynomial in \(y\) times the same Gaussian, plus an \(O(S^{-1/2})\) coefficient perturbation.

Thus for every fixed source/curl/frame derivative order \(N_0\),

\[
\boxed{
\sup_{r\in I_S^{\rm cen}}
\frac{
|\partial_{\rm norm}^{\le N_0}A_{S,r}|
}{
|A_{S,r}|_{\rm diag}
}
\le
C_{N_0}(1+L_S)^{D_{N_0}}
\le
C_{N_0}(\log S)^{D_{N_0}/2}.
}
\tag{GC16}
\]

Here \(A_{S,r}\) denotes the full slowly varying amplitude entering the physical \(H^1\) Gram integrand after the common carrier has been cancelled.

Equation (GC16) is the needed **relative-jet** estimate.  It is proved only on the moving central window, where the explicit nonvanishing Gaussian profile is used.  No relative estimate is asserted in the far tail.

The exact discrete perturbation \(w_{C,S}\) contributes \(o(1)\) in the same graph norm by (GC5)--(GC7), so (GC16) persists with one additional harmless factor \(1+o(1)\).

---

## 6. Exact radial phase separation

For catalyst orbit characters,

\[
C_r-C_s=(r-s)M.
\]

The exact root-phase theorem gives

\[
\partial_R(\Theta_{C_r}-\Theta_{C_s})
=
(r-s)(\Xi_M\cdot e_R)
\tag{GC17}
\]

with no stationary point and

\[
\boxed{
|\Xi_M|
\asymp
\frac1{\sqrt\varepsilon S}.
}
\tag{GC18}
\]

Therefore the radial integration-by-parts operator is

\[
\mathcal L_{r,s}
=
\frac1{i(r-s)(\Xi_M\cdot e_R)}
\partial_R.
\tag{GC19}
\]

Compact source cutoffs remove boundary terms.

Applying (GC19) \(N\) times and using (GC16) gives, uniformly for distinct \(r,s\in I_S^{\rm cen}\),

\[
\boxed{
\frac{
|\langle\nabla W_{C,r},\nabla W_{C,s}\rangle|
}{
\|\nabla W_{C,r}\|_2
\|\nabla W_{C,s}\|_2
}
\le
C_N(\log S)^{D_N}
\left(
\frac{\sqrt\varepsilon S}{|r-s|}
\right)^N
+o(1)_{\rm disc}.
}
\tag{GC20}
\]

The \(o(1)_{\rm disc}\) term is the exact discrete-profile perturbation and is controlled in operator norm through (GC5), not added pairwise.

---

## 7. Schur coercivity on the moving central window

Define the normalized central Gram off-diagonal kernel \(K_{rs}\).

For \(N>1\), (GC20) gives

\[
\sup_{r\in I_S^{\rm cen}}
\sum_{\substack{s\in I_S^{\rm cen}\\s\ne r}}
|K_{rs}|
\le
C_N(\log S)^{D_N}
(\sqrt\varepsilon S)^N
\sum_{d\ne0}|d|^{-N}
+o(1).
\tag{GC21}
\]

On the constant-fast Gate 2 schedule,

\[
\varepsilon_j\asymp j^{-1},
\qquad
S_j\asymp(\log j)^2,
\]

so

\[
\sqrt{\varepsilon_j}S_j
\asymp
\frac{(\log j)^2}{\sqrt j}.
\tag{GC22}
\]

Hence, for any fixed \(N>1\),

\[
\boxed{
(\log S_j)^{D_N}
(\sqrt{\varepsilon_j}S_j)^N
\to0.
}
\tag{GC23}
\]

Therefore

\[
\boxed{
\|K_j^{\rm cen}\|_{\ell^2\to\ell^2}
=o(1).
}
\tag{GC24}
\]

For all sufficiently late \(j\),

\[
\boxed{
\left\|
\sum_{r\in I_{S_j}^{\rm cen}}
\nabla W_{C,r}
\right\|_2^2
\ge
\frac34
\sum_{r\in I_{S_j}^{\rm cen}}
\|\nabla W_{C,r}\|_2^2.
}
\tag{GC25}
\]

In particular the central \(r=0\) packet cannot be cancelled by the other central catalyst orbit modes.

---

## 8. Reinsert the far tail and exact discrete perturbation

From (GC14),

\[
\left\|
\sum_{r\in I_S^{\rm far}}\nabla W_{C,\phi,r}
\right\|_2
=o\!\left(
q^{-1/4-3h/2}
\right).
\tag{GC26}
\]

From (GC5)--(GC7), the exact discrete-profile correction contributes

\[
\boxed{
\|\nabla(W_{C,f_S}-W_{C,\phi})\|_2
=
o\!\left(
q^{-1/4-3h/2}
\right)
}
\tag{GC27}
\]

in the same physical tangent-Gaussian graph realization.

Combining (GC25)--(GC27) with the repaired diagonal lower bound gives

\[
\boxed{
\|\nabla V_j\|_2
\ge
cS_j^{-A_0/2}q_j^{-1/4-3h/2}.
}
\tag{GC28}
\]

The polynomial factor comes only from the robust center-point localization used in the lower-bound audit; it is not a Gram-matrix loss.

---

## 9. Consequence for the full Gate 3 state

The beta-one angular projection separates the catalyst sector from the beta-two parent and beta-zero mean sectors.

The analytic reset tail and residual correction satisfy

\[
\|T_j+Z_j\|_{H^1}
=
o\!\left(
S_j^{-A_0/2}q_j^{-1/4-3h/2}
\right)
\tag{GC29}
\]

by the already-audited phase-adapted physical upper bounds.

Thus

\[
\boxed{
\|U_j^*\|_{H^1}
\ge
cS_j^{-A_0/2}q_j^{-1/4-3h/2}.
}
\tag{GC30}
\]

With

\[
q_j\asymp j^{-1/h},
\qquad
S_j\asymp(\log j)^2,
\]

the right side diverges.

Therefore the microlocal cancellation issue isolated in Audit G is closed inside the present theorem chain.

---

## 10. Gate 3 audit update

The adversarial dependency audit had one open mathematical line:

\[
\text{physical catalyst-orbit }H^1\text{ Gram coercivity}.
\]

Sections 1--9 close it by avoiding pairwise normalization in the physical far tail.

Hence, at the internal branch level,

\[
\boxed{
\text{Audit G: PASS.}
}
\tag{GC31}
\]

and there is no remaining **known** Gate 3 construction gap in the theorem-layer dependency graph.

Because the assembled conclusion is at the level of the three-dimensional unforced Navier--Stokes singularity problem, the next action must not be another theorem-strengthening step.  It must be a conservative source-by-source verification of the entire chain, with every non-source theorem classified as

- exact pinned-source theorem;
- standard external PDE theorem;
- derived branch lemma with complete proof;
- numerical certificate only;
- still conditional.

The preferred next file is

\[
\boxed{
\texttt{beta21_lowu_global_claim_source_line_audit.md}.
}
\]

No Clay-prize or final public blow-up claim is made in this file.
