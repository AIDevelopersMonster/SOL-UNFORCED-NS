# Exact finite-`S` core+analytic-tail local reset theorem

**Status:** SUPERSEDED / CONDITIONAL. THE ADVERSARIAL GATE 1 RE-AUDIT (`beta21_lowu_gate1_adversarial_reaudit.md`) FOUND UNPROVED INPUTS IN THE FINITE-S NORMAL FORM, DISCRETE-PROFILE INVERSION, COMMON-NORM DERIVATIVE LEDGER, THREE-BLOCK CONTRACTION, NUMERICAL CERTIFICATION, AND FINITE-DIMENSIONAL LYAPUNOV--SCHMIDT LEDGER. THIS FILE MUST NOT BE CITED AS A CLOSED GATE 1 THEOREM UNTIL THOSE OBLIGATIONS ARE REPAIRED.

This theorem replaces the superseded compact-bank interpretation of
`beta21_lowu_exact_finiteS_local_reset.md`.

The compact-primary-bank obstruction proves that no nonzero finitely supported designated orbit can be an exact reset fixed state.  The correct local state is

\[
\boxed{
U_S
=U_{base,S}+M_{des,S}
+U_{core,S}
+z_{tail,S}
+m_{corr,S}+z_{nz,S},
}
\tag{G1}
\]

where

- `U_core,S` is the exact finite-`S` tangent-Gaussian beta-one/beta-two central orbit profile;
- `z_tail,S` is the analytic lattice completion required by compact support;
- `m_corr,S` and `z_nz,S` are the residual mean and nonzero-harmonic PDE corrections;
- `M_des,S` is the designated beta-zero Cauchy root.

For every sufficiently large `S`, the macroscopic parameters can be chosen so that the full field has zero unforced Navier--Stokes residual on the local relay collar and the complete core+tail state is exactly fixed by the one-cell Poincare/reset map.

The theorem is **local**.  It does not prove an infinite cascade, a global singular solution, or the Navier--Stokes Millennium problem.

## 1. Ingredients already proved

The proof uses the following theorem layers.

### 1.1 Principal low-`u` mechanism

At

\[
u=1.8,\qquad x=0.70,
\]

the beta-one/beta-two bilateral orbit is generated by the invariant beta-zero character

\[
M=P-2C,
\qquad R(M)=M.
\]

The boundary-layer spacing is

\[
\delta_S=\kappa/S.
\]

The limiting central reset point has

\[
\kappa_*=0.894049167184884\ldots,
\qquad
\theta_*=2.619416765747896\ldots.
\]

The catalyst and parent dispersion equations are transverse.

### 1.2 One physical root supplies both shift sectors

`beta21_lowu_single_root_coupling_ratio.md` and
`beta21_lowu_source_growing_coordinate_normalization.md`
prove that one scalar root amplitude and one transverse polarization parameter can realize both principal shift strengths with no hidden beta-sector normalization obstruction.

### 1.3 Exact discrete central profiles

`beta21_lowu_exact_discrete_central_profile.md`
proves, after one normalization per sector, exact tangent-normalized profiles

\[
\mathcal P_{b,S}^{tan}f_{b,S}=f_{b,S},
\]

with

\[
\boxed{
f_{b,S}=f_b+O(S^{-1/2})
}
\tag{G2}
\]

in the physical Gaussian graph norm.

### 1.4 Compact-bank boundary defect

Cutting these smooth profiles to the actual source-supported primary-wave bank creates

\[
\boxed{
\|d_{bd,S}\|
\le S^Ae^{-cS}.
}
\tag{G3}
\]

This is the precise defect isolated by
`beta21_lowu_compact_orbit_exact_reset_obstruction.md`.

### 1.5 Polynomial analytic-tail right inverse

`beta21_lowu_finiteS_tail_parametrix_and_secondary_resonance.md`
constructs the finite-`S` analytic-tail right inverse

\[
\boxed{
\mathcal G_{tail,S}
}
\]

with

\[
\boxed{
\|\mathcal G_{tail,S}\|
\le CS^{A_t}
}
\tag{G4}
\]

for a fixed finite exponent `A_t` (at principal level one may take `A_t=1/2`; fixed source/curl derivative bookkeeping may enlarge this exponent).

The secondary parent resonance contributes only a finite-dimensional Gaussian gauge and at most the polynomial loss already included in (G4).

### 1.6 Exact residual PDE correction

`beta21_lowu_coupled_mean_nonzero_contraction.md`
proves the coupled residual mean/nonzero Banach solve around a prescribed designated low-`u` state.  Its diagonal Lipschitz constants tend to zero; the one non-small mean-to-wave leg is paired with a source-small return leg so the product tends to zero.

### 1.7 `C^1` transfer

`beta21_lowu_coupled_C1_parameter_transfer.md`
proves that fixed polynomial losses in `S` do not destroy the source-small correction or principal finite-dimensional transversality.

## 2. Unknowns for the corrected local problem

Fix the exact normalized central profiles and write the compact designated core as

\[
U_{core,S}^{cut}.
\]

Let

\[
T\in X_{tail,S},
\qquad
m\in Y_S,
\qquad
z\in Z_S
\]

be respectively:

- the analytic reset tail;
- the residual angular mean;
- the residual nonzero-angular correction.

The full infinite-dimensional unknown is

\[
\boxed{W=(T,m,z).}
\tag{G5}
\]

For fixed macroscopic parameter vector

\[
p=(\kappa,\theta,A_M,\tau)
\]

in a sufficiently small compact neighborhood of the principal point, define one coupled iteration

\[
\boxed{
W'=\mathfrak T_S(p;W).
}
\tag{G6}
\]

The tail component solves the Poincare mismatch using `G_tail,S`; the mean/nonzero components use the already constructed forward Duhamel propagators.

## 3. Inhomogeneous radius

At `W=0`, the forcing consists of:

1. the compact-bank boundary defect (G3);
2. ordinary source-small residual mean/covariance terms;
3. off-orbit nonzero leakage;
4. finite-`S` frame/curl/reconstruction remainders.

All of these obey

\[
\boxed{
R_S^
\le
S^A\varepsilon^{a_*}
+S^Ae^{-cS},
\qquad a_*>0.
}
\tag{G7}
\]

before applying the tail inverse.

By (G4), the tail response is bounded by

\[
\boxed{
\|T^{(0)}_S\|_{tail}
\le
S^{A+A_t}\varepsilon^{a_*}
+S^{A+A_t}e^{-cS}.
}
\tag{G8}
\]

Since the source hierarchy makes every fixed polynomial in `S` negligible against a fixed positive epsilon exponent, and of course against `e^{-cS}`, we still have

\[
\boxed{
R_{*,S}:=
S^{A'}\varepsilon^{a_*}
+S^{A'}e^{-cS}
\longrightarrow0.
}
\tag{G9}
\]

for one enlarged fixed exponent `A'`.

## 4. Linearized block matrix

For two states `W_1,W_2`, write the norm vector

\[
\mathbf d
=
\begin{pmatrix}
\|\delta T\|_{tail}\\
\|\delta m\|_{Y_S}\\
\|\delta z\|_{Z_S}
\end{pmatrix}.
\]

The source-localized bilinear, covariance and off-orbit estimates give a positive block bound

\[
\mathbf d'
\le L_S\mathbf d,
\tag{G10}
\]

where schematically

\[
L_S=
\begin{pmatrix}
q_{T,S}&a_{Tm,S}&a_{Tz,S}\\
a_{mT,S}&q_{m,S}&b_S\\
a_{zT,S}&c_S&q_{z,S}
\end{pmatrix}.
\tag{G11}
\]

The known mean/nonzero entries satisfy

\[
q_{m,S}\to0,
\qquad
q_{z,S}\to0,
\qquad
b_Sc_S\to0.
\tag{G12}
\]

For the new tail entries, every path involving a tail perturbation contains at least one of:

- the polynomial tail inverse `S^{A_t}` followed by a source-small nonlinear coefficient;
- an action-gap factor `e^{-cS}`;
- a covariance gain with positive epsilon power;
- the central/tail cutoff commutator `O(S^{-1/2})`.

Hence

\[
\boxed{
q_{T,S}\to0,
}
\tag{G13}
\]

and for every directed cycle in the three-block graph the product of its off-diagonal Lipschitz entries tends to zero.  In particular,

\[
a_{Tm,S}a_{mT,S}\to0,
\]

\[
a_{Tz,S}a_{zT,S}\to0,
\]

\[
b_Sc_S\to0,
\]

and the two three-cycles also tend to zero.

Therefore the Perron root of the nonnegative matrix satisfies

\[
\boxed{
\rho(L_S)\to0.
}
\tag{G14}
\]

This statement does **not** require every individual off-diagonal entry to tend to zero; as in the previous two-block theorem, polynomial one-way sensitivities are permitted when their return path is source-small.

## 5. Product norm and Banach fixed point

For sufficiently large `S`, choose a positive left Perron weight vector

\[
w_S=(w_T,w_m,w_z)>0
\]

for a matrix dominating `L_S`, normalized arbitrarily.  Define

\[
\boxed{
\|W\|_{*,S}
=w_T\|T\|_{tail}
+w_m\|m\|_{Y_S}
+w_z\|z\|_{Z_S}.
}
\tag{G15}

By (G14), for all sufficiently large `S`,

\[
\boxed{
\|\mathfrak T_S(p;W_1)-
\mathfrak T_S(p;W_2)\|_{*,S}
\le q_{*,S}
\|W_1-W_2\|_{*,S},
\quad q_{*,S}<1/2.
}
\tag{G16}
\]

The inhomogeneous estimate (G9) implies that the ball

\[
\boxed{
\|W\|_{*,S}
\le2R_{*,S}
}
\tag{G17}
\]

is invariant.

Hence Banach's fixed-point theorem gives a unique local correction

\[
\boxed{
W_S(p)
=(T_S(p),m_S(p),z_S(p)).
}
\tag{G18}

The tail satisfies in particular

\[
\boxed{
\|T_S\|_{tail}
\le
S^{A'}\varepsilon^{a_*}
+S^{A'}e^{-cS}.
}
\tag{G19}

## 6. What the fixed point solves

The fixed equation (G18) was built from the exact source/physical propagators and the exact one-cell Poincare defect.  Therefore it imposes simultaneously:

1. zero unforced physical Navier--Stokes residual on the local relay collar;
2. exact completion of the compact designated core by the analytic tail;
3. exact mean/nonzero complement equations;
4. exact equality of the full outgoing core+tail state with the reset-relabeled incoming state, up to the remaining finite macroscopic reset observables.

The earlier compact-bank obstruction is respected because the exact state is not finitely supported.

## 7. `C^1` dependence after adding the tail

Parameter differentiation of the new tail block costs at most the polynomial factor in (G4), together with the already recorded finite powers from source coefficient derivatives.

Every differentiated inhomogeneous source still contains a positive epsilon exponent or an action gap.  Therefore, exactly as in the previous `C^1` transfer theorem,

\[
\boxed{
\|W_S(p)\|_{*,S}
+\|D_pW_S(p)\|_{*,S}
=o(1)
}
\tag{G20}

strongly enough to absorb every fixed polynomial output trace loss.

Thus the exact core+tail/PDE-corrected finite-dimensional reset observable obeys

\[
\boxed{
\mathcal O_S^{full}(p)
=
\mathcal O_S^{principal}(p)+o(1),
}
\tag{G21}

and

\[
\boxed{
D_p\mathcal O_S^{full}(p)
=
D_p\mathcal O_S^{principal}(p)+o(1).
}
\tag{G22}

## 8. Finite-dimensional transversality

The principal catalyst derivative is

\[
\lambda_C-\rho_C^{-1}
=-5.83150782219\ldots\ne0.
\]

The parent two-real-dimensional block has

\[
\det D_{(\kappa,\theta)}
(\Re F_P,\Im F_P)
=1.2199224287\ldots\ne0.
\]

The root polarization ratio derivative is also nonzero:

\[
\mathfrak R'(\tau_*)
=\frac{2}{(1+2\tau_*)^2}\ne0.
\]

After one harmless normalization/gauge choice in the discrete profile and the secondary parent tail resonance, the finite-dimensional parameter ledger has the same number of genuine normalization equations and physical parameters.  The principal Jacobian is nonsingular on the required observable block.

By (G22), the exact full Jacobian remains nonsingular for all sufficiently large `S`.

## 9. Exact local parameter choice

Let `p_S^{prin}` be the already constructed finite-`S` principal parameter vector, satisfying

\[
\boxed{
p_S^{prin}=p_*+O(S^{-1}).}
\tag{G23}

At this point the full correction produces only a source-small observable defect:

\[
\mathcal O_S^{full}(p_S^{prin})=o(1).
\]

Since the exact Jacobian is uniformly nonsingular, the finite-dimensional implicit-function theorem gives

\[
\boxed{
p_S^{exact}
=p_S^{prin}+o(1)
}
\tag{G24}

such that

\[
\boxed{
\mathcal O_S^{full}(p_S^{exact})=0.
}
\tag{G25}

Because the correction is source-small compared with the already present `O(S^{-1})` principal drift, one may retain the coarse asymptotic statement

\[
\boxed{
p_S^{exact}=p_*+O(S^{-1}).}
\tag{G26}

## 10. Gate 1 theorem

Combining sections 1--9 gives:

### Theorem (exact finite-`S` low-`u` core+tail local reset)

For all sufficiently large source levels `S`, there exists a choice of low-`u` boundary-layer parameters and a smooth divergence-free incoming Cauchy state of the form (G1) such that:

1. the physical viscosity-one incompressible Navier--Stokes residual is exactly zero on the local relay collar;
2. the beta-one/beta-two central designated orbit is the exact discrete tangent-Gaussian profile from (G2);
3. the compact source support is completed by a source-small analytic tail satisfying (G19);
4. the residual angular mean and nonzero-harmonic complements solve their exact coupled equations;
5. after the canonical reset relabelling, the **full core+tail state** at the outgoing face equals the incoming full state;
6. all construction parameters remain within `o(1)` of the low-`u` principal point, with the coarse bound (G26).

Symbolically,

\[
\boxed{
\mathscr P_S
\left(
U_{core,S}+T_S+m_S+z_S
\right)
=
U_{core,S}+T_S+m_S+z_S.
}
\tag{G27}

This is the corrected exact local theorem that replaces the impossible compact-bank fixed-vector claim.

## 11. What is now closed

The following local obstacles are closed in the current theorem chain:

- dangerous high-beta low-`u` action competition on the selected core;
- single-root catalyst/parent coupling compatibility;
- exact Cauchy/curl realization of the beta-zero root;
- source-to-physical viscosity-one residual matching;
- anisotropic whole-space Leray control;
- root-augmented mean Oseen propagation;
- growing-orbit covariance counting;
- nonzero off-orbit propagation;
- coupled residual mean/nonzero zero-residual solve;
- compact-primary-bank obstruction;
- analytic-tail spectral separation;
- secondary parent tail resonance;
- exact finite-`S` discrete central profile;
- exact finite-`S` core+tail local reset.

## 12. Gate 2 is now the unique frontier

No global conclusion follows from (G27).  The spectral stability audit already proves that arbitrary small inter-cell mismatch cannot simply be accumulated: the limiting reset has expanding transverse directions.

The next theorem is therefore not another local correction theorem.  It is

\[
\boxed{
\textbf{Gate 2: exact scale-dependent inter-cell invariant cocycle.}
}
\tag{G28}

One must construct a sequence of full core+tail states

\[
U_j^{inv}
\]

such that physical gap transport and the exact local map satisfy

\[
\boxed{
U_{j+1}^{inv}
=\mathcal F_j(U_j^{inv})
}
\tag{G29}

with no transverse mismatch in the unstable directions.

Only after Gate 2 is closed should the programme address one global Cauchy datum, infinite cascade assembly, finite energy and singularity formation.

No such global theorem is claimed here.
