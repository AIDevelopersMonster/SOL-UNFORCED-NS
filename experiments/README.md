# Experiments

Run:

```bash
python3 relay_parameter_scan.py
```

Expected v0.1 residuals are on the order of \(10^{-10}\) for the stored rounded witness.

These experiments validate only reduced-model arithmetic. They are not PDE proofs.
