# Results

`relay_candidates.csv` stores reduced-model candidate parameters.

A row here is a **numerical witness only**. It is not a theorem and should not be cited as a PDE result without the corresponding proof.
